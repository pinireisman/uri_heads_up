# Decision Log

| #   | Date       | Decision                                                                                                                                                                                                              | Why                                                                                                                       |
| --- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| D1  | 2026-07-10 | **UI languages: English + Hebrew**, full RTL support, language switcher.                                                                                                                                              | Owner choice (PRD Q5, overrides default). Layout must be direction-agnostic from the first component.                     |
| D2  | 2026-07-10 | **Deploy to GitHub Pages.**                                                                                                                                                                                           | Owner choice (PRD Q17). Free static HTTPS; motion sensors require HTTPS.                                                  |
| D3  | 2026-07-10 | **Lean solo workflow**, not the PRD's 8-agent process. Artifacts: this file + IMPLEMENTATION_PLAN.md. Phase order and gates kept from the PRD.                                                                        | Owner choice. Same app, less overhead.                                                                                    |
| D4  | 2026-07-10 | **Real-device testing on Android Chrome only.** iPhone Safari permission flow (`DeviceOrientationEvent.requestPermission()`) is implemented but ships untested; touch fallback covers iPhones until one is available. | Owner has no iPhone for testing. Release criteria in PRD §12.4 are scoped to Android accordingly.                         |
| D5  | 2026-07-10 | **Stack: Vite + React + TypeScript (strict) + Vitest.** Playwright added in Phase 3 for the synthetic-sensor E2E scenarios.                                                                                           | PRD §10.1 recommendation; no reason to deviate.                                                                           |
| D6  | 2026-07-10 | **Hash-based routing** (`#/play` etc.).                                                                                                                                                                               | GitHub Pages has no SPA rewrite rules; hash routing avoids 404s on refresh with zero config.                              |
| D7  | 2026-07-10 | **IndexedDB via the `idb` package** behind a repository interface (PRD FR-9).                                                                                                                                         | Tiny promise wrapper; hand-rolling IDB transactions is where bugs live.                                                   |
| D8  | 2026-07-10 | **Zod for import JSON validation.**                                                                                                                                                                                   | Import is a trust boundary (PRD §15); a schema library is less code and more correct on edge cases than a hand validator. |
| D9  | 2026-07-10 | **Hand-rolled i18n**: typed `t()` over two locale dictionaries + `dir` attribute switching. No i18next.                                                                                                               | Two locales, small string count. Add a library only if pluralization/interpolation needs outgrow it.                      |
| D10 | 2026-07-10 | **PWA via `vite-plugin-pwa`** (Workbox precache).                                                                                                                                                                     | Standard, maintained, handles the Pages base path.                                                                        |

## PRD open questions (§18) — resolved

Q5 → English + Hebrew UI (D1). Q14 → Android Chrome primary; others via touch fallback (D4). Q17 → GitHub Pages (D2). **All other questions: PRD defaults accepted as written** (no timer, end on manual/exhaustion, no teams, local-only storage, editable imports, no admin lock, name/description/emoji/color metadata, phrases up to 200 chars, no in-round repeats, optional sounds off by default, tilt-down = correct with invert setting, keep last 20 rounds, one demo starter category, offline required).
